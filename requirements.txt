# Wayfinder Aura - Python Dependencies
# Install with: pip install -r requirements.txt

# UI Framework
customtkinter>=5.2.0

# HTTP requests for model downloads
requests>=2.28.0

# Glassmorphic Status Overlay (PyQt6)
# Premium overlay with blur effects, animations, and squircle shape
PyQt6>=6.5.0

# Audio Recording
sounddevice>=0.4.6
numpy>=1.24.0

# Audio Preprocessing (optional but recommended for better accuracy)
scipy>=1.10.0

# Input Device Monitoring (Linux)
evdev>=1.6.0

# System Tray
pystray>=0.19.0
Pillow>=10.0.0

# Optional: D-Bus for Wayland GlobalShortcuts
# dbus-python  # Usually system-installed
# PyGObject    # Usually system-installed

# Optional: GPU-accelerated transcription with Faster-Whisper
# Requires PyTorch with ROCm (AMD) or CUDA (NVIDIA) support
# Install PyTorch first: pip install torch --index-url https://download.pytorch.org/whl/rocm6.0
# Then install faster-whisper:
# faster-whisper>=1.0.0

# LLM Post-Processing (cleanup filler words, smart formatting)
# Local inference with llama.cpp (fast, private, no cost) - DEFAULT
# Recommended models: Qwen2.5-1.5B-Instruct, Phi-3-mini, SmolLM2-1.7B
# Download GGUF models from: https://huggingface.co/models?sort=trending&search=gguf
llama-cpp-python>=0.3.0

# Optional: Cloud inference with Anthropic Claude (high quality, requires API key)
# Get API key from: https://console.anthropic.com/
# anthropic>=0.40.0

# Cloud inference with OpenAI (GPT models + Whisper transcription, requires API key)
# Get API key from: https://platform.openai.com/api-keys
openai>=1.0.0

# Ultra-fast cloud transcription with Groq (Whisper Large-v3 on LPU hardware)
# ~10x faster than standard Whisper API, same accuracy
# Get API key from: https://console.groq.com/
groq>=0.4.0
