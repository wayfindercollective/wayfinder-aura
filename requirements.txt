# Wayfinder Aura - Python Dependencies
# Install with: pip install -r requirements.txt
#
# This file lists only deps that ship as prebuilt wheels for the supported
# platforms (manylinux x86_64, macOS arm64, Windows). It is safe to install on
# environments without a C toolchain — notably SteamOS, whose rootfs ships no
# gcc/cmake/kernel-headers (see STEAMDECK-INSTALL-LOG.md issues 5–7).
#
# Source-build-only and platform-restricted deps live in requirements-optional.txt.
# Install both together on a system with a working C toolchain:
#     pip install -r requirements.txt -r requirements-optional.txt

# UI Framework
customtkinter>=5.2.0

# HTTP requests for model downloads + online license activation
requests>=2.28.0

# Ed25519 signature verification for online license tokens (installs a prebuilt wheel — no Rust)
cryptography>=41.0.0

# Glassmorphic Status Overlay (PyQt6)
# Premium overlay with blur effects, animations, and squircle shape
PyQt6>=6.5.0

# Audio Recording
sounddevice>=0.4.6
numpy>=1.24.0

# Audio Preprocessing (optional but recommended for better accuracy)
scipy>=1.10.0

# Input Device Monitoring on Linux is provided by `evdev`, but pip-installing it
# requires linux/input.h kernel headers — stripped on SteamOS (Issue 5). It is
# moved to requirements-optional.txt; on SteamOS, install `python-evdev` via
# pacman instead and run the venv with --system-site-packages.

# Cross-platform hotkey listener
pynput>=1.7.6

# Text injection on macOS (pyautogui)
pyautogui>=0.9.54; sys_platform == 'darwin'

# System Tray
pystray>=0.19.0
Pillow>=10.0.0

# Optional: D-Bus for Wayland GlobalShortcuts
# dbus-python  # Usually system-installed
# PyGObject    # Usually system-installed

# Optional: GPU-accelerated transcription with Faster-Whisper
# Requires PyTorch with ROCm (AMD) or CUDA (NVIDIA) support
# Install PyTorch first: pip install torch --index-url https://download.pytorch.org/whl/rocm6.0
# Then install faster-whisper:
# faster-whisper>=1.0.0

# LLM Post-Processing (cleanup filler words, smart formatting) — local inference
# with llama.cpp lives in requirements-optional.txt because llama-cpp-python has
# no prebuilt wheel on PyPI and needs gcc + cmake to build from source, which
# SteamOS lacks (Issue 7). Cloud-only and whisper.cpp-only setups don't need it.

# Optional: Cloud inference with Anthropic Claude (high quality, requires API key)
# Get API key from: https://console.anthropic.com/
# anthropic>=0.40.0

# Cloud inference with OpenAI (GPT models + Whisper transcription, requires API key)
# Get API key from: https://platform.openai.com/api-keys
openai>=1.0.0

# Ultra-fast cloud transcription with Groq (Whisper Large-v3 on LPU hardware)
# ~10x faster than standard Whisper API, same accuracy
# Get API key from: https://console.groq.com/
groq>=0.4.0
